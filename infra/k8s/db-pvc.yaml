# NOTE: 動作未検証。学内サーバー移行時の参考構成です。
# docker-compose.yml をベースに作成しています。
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: lab-rag
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
